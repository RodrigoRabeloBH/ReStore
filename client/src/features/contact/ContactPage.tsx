import { Typography } from "@mui/material"

const ContactPage = () => {
    return (
        <Typography variant="h6">
            Contact page
        </Typography>
    );
}

export default ContactPage;